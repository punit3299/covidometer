import { StatesearchPipe } from './statesearch.pipe';

describe('StatesearchPipe', () => {
  it('create an instance', () => {
    const pipe = new StatesearchPipe();
    expect(pipe).toBeTruthy();
  });
});
