import { DistrictsearchPipe } from './districtsearch.pipe';

describe('DistrictsearchPipe', () => {
  it('create an instance', () => {
    const pipe = new DistrictsearchPipe();
    expect(pipe).toBeTruthy();
  });
});
